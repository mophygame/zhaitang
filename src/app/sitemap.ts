import type {MetadataRoute} from "next";import {properties} from "@/data/properties";export const dynamic="force-static";export default function sitemap():MetadataRoute.Sitemap{const base="https://zhaitang.example";return ["","/properties","/staff","/paranormal-photo",...properties.map(p=>`/properties/${p.id}`)].map(url=>({url:base+url,lastModified:new Date()}))}
