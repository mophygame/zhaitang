import {CommissionProvider} from "@/components/shared/UI";import {PhoneCallProvider} from "@/components/shared/PhoneCall";import {BackToTop} from "@/components/shared/BackToTop";import {Header} from "./Header";import {Footer} from "./Footer";
export function SiteShell({children}:{children:React.ReactNode}){return <PhoneCallProvider><CommissionProvider><Header/><main>{children}</main><Footer/><BackToTop/></CommissionProvider></PhoneCallProvider>}
